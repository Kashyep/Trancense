import Link from "next/link"; import { Badge } from "@/components/ui"; import { ThemeToggle } from "@/components/theme-toggle";
const links=[['Dashboard','/app'],['Add & review data','/app/energy'],['Import data','/app/import'],['Equipment','/app/equipment'],['Documents','/app/evidence'],['Analyse','/app/analysis'],['Findings & actions','/app/findings'],['Solar planning','/app/solar'],['Assistant','/app/assistant'],['Reports','/app/reports'],['Settings','/app/settings']];
export function AppShell({children,workspace,role}:{children:React.ReactNode;workspace:string;role:string}){return <div className="app-grid md:grid md:grid-cols-[238px_minmax(0,1fr)]"><aside className="desktop-nav editorial-rail p-6"><Link href="/" className="brand">Trancense<span>.</span></Link><div className="mt-12 border-y border-[var(--border)] py-5"><p className="kicker">Workspace</p><p className="mt-2 text-sm">{workspace}</p><Badge tone="good">{role}</Badge></div><nav aria-label="Workspace" className="mt-6 space-y-1">{links.map(([label,href],i)=><Link key={href} href={href} className="app-nav-link flex gap-3 px-2 py-2 text-sm"><span className="text-[var(--muted-foreground)]">{String(i+1).padStart(2,'0')}</span>{label}</Link>)}</nav></aside><div className="app-main min-w-0"><header className="app-header"><div className="app-context"><strong>{workspace}</strong><span className="ml-2">Private audit workspace · {role}</span></div><div className="flex items-center gap-3"><ThemeToggle /><form action="/auth/signout" method="post"><button className="button button-secondary min-h-9 px-3 text-sm">Sign out</button></form></div></header>{children}<nav aria-label="Mobile workspace" className="mobile-nav fixed bottom-0 z-10 w-full justify-around border-t p-2">{links.slice(0,5).map(([label,href])=><Link className="p-2 text-xs" key={href} href={href}>{label.split(' ')[0]}</Link>)}</nav></div></div>}
