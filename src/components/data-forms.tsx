"use client";
import { useActionState } from "react"; import { createEnergyRecord, uploadEvidence } from "@/app/actions"; import { Button } from "@/components/ui";
const initial={ok:false,error:""};
export function EnergyForm({workspaceId,auditId,boundaryId}:{workspaceId:string;auditId:string;boundaryId:string}){const [state,action,pending]=useActionState(createEnergyRecord,initial);return <form action={action} className="utility-card p-6"><input type="hidden" name="workspaceId" value={workspaceId}/><input type="hidden" name="auditId" value={auditId}/><input type="hidden" name="boundaryId" value={boundaryId}/><p className="kicker">Monthly input</p><h2 className="mt-2 text-3xl">Add monthly energy data</h2><p className="mt-2 muted">Draft records never enter trusted calculations. Electricity in kWh is normalized directly; other units need an approved factor.</p><div className="mt-5 grid gap-4 sm:grid-cols-2">{[['periodStart','Period starts','date'],['periodEnd','Period ends','date'],['quantity','Original quantity','number'],['cost','Total cost (INR, optional)','number']].map(([name,label,type])=><label className="form-label mt-0" key={name}>{label}<input required={name!=='cost'} name={name} type={type} min="0" step="any" className="form-input"/></label>)}<label className="form-label mt-0">Source<select name="source" className="form-select"><option value="electricity">Electricity / grid power</option><option value="diesel">Diesel</option><option value="petrol">Petrol</option><option value="lpg">LPG</option><option value="natural_gas">Natural gas</option><option value="renewable">Renewable generation</option><option value="other">Other</option></select></label><label className="form-label mt-0">Original unit<input name="unit" defaultValue="kWh" className="form-input"/></label></div><label className="form-label">Notes<textarea name="notes" className="form-textarea"/></label><Button className="mt-5" disabled={pending}>{pending?'Saving…':'Save draft record'}</Button>{state.error&&<p role="alert" className="field-error mt-3">{state.error}</p>}</form>}
export function EvidenceForm({workspaceId,auditId}:{workspaceId:string;auditId:string}){const [state,action,pending]=useActionState(uploadEvidence,initial);return <form action={action} className="utility-card p-6"><input type="hidden" name="workspaceId" value={workspaceId}/><input type="hidden" name="auditId" value={auditId}/><p className="kicker">Private document</p><h2 className="mt-2 text-3xl">Add source evidence</h2><p className="mt-2 muted">PDF, PNG, JPEG, CSV, or XLSX up to 10 MB. Files are private, treated as untrusted, and require review before they support approved data.</p><label className="form-label">File<input required name="file" type="file" accept="application/pdf,image/png,image/jpeg,text/csv,application/vnd.openxmlformats-officedocument.spreadsheetml.sheet" className="form-input"/></label><label className="form-label">Source / provider<input name="source" className="form-input"/></label><label className="form-label">Notes<textarea name="notes" className="form-textarea"/></label><Button className="mt-5" disabled={pending}>{pending?'Uploading…':'Upload evidence'}</Button>{state.error&&<p role="alert" className="field-error mt-3">{state.error}</p>}</form>}
