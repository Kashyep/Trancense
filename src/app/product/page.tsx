import { PublicPage } from "@/components/public-page"; import { ActionLink } from "@/components/ui"; import { productContent } from "@/content/product";
export default function Product(){return <PublicPage><section className="shell section"><p className="kicker">The Trancense platform</p><h1 className="mt-4 max-w-3xl">Energy audit work that remains legible under review.</h1><p className="mt-7 max-w-2xl text-lg muted">The platform follows the audit trail from source evidence through approved inputs, reproducible analysis, reviewed conclusions, and accountable action.</p></section><section className="section-band section-band-muted"><div className="shell section feature-list">{productContent.features.map(([title,body],i)=><article key={title}><span>0{i+1}</span><div><h2 className="text-2xl">{title}</h2><p>{body}</p></div></article>)}</div></section><section className="shell section"><ActionLink href="/contact">Request a pilot conversation</ActionLink></section></PublicPage>}
