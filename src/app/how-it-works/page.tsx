import { siteContent } from "@/content/site"; import { ActionLink } from "@/components/ui";
export default function How(){return <main><section className="shell section"><p className="eyebrow">How it works</p><h1 className="mt-5 max-w-3xl">A chain that makes the next decision easier to examine.</h1></section><section className="ivory-band"><ol className="shell section grid gap-px bg-[#d8d0c3] md:grid-cols-2 lg:grid-cols-3">{siteContent.chain.map((step,index)=><li className="bg-[#ebe6dd] p-7" key={step}><p className="eyebrow">0{index+1}</p><h2 className="mt-10 text-3xl">{step}</h2><p className="mt-4 muted">{index===0?'Preserve source context and original units.':index===1?'Separate reviewed compatible records from draft work.':index===2?'Show formulas, factors, scope, and exclusions.':index===3?'Keep support and confidence visible.':index===4?'Assign and track work without implying measured outcomes.':'Record measured verification distinctly when it exists.'}</p></li>)}</ol></section><section className="shell section"><ActionLink href="/product" variant="secondary">Explore the platform</ActionLink></section></main>}
