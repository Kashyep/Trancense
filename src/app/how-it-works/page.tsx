import type { Metadata } from "next"; import { siteContent } from "@/content/site"; import { ActionLink } from "@/components/ui"; import { PublicPage } from "@/components/public-page";
export const metadata: Metadata = { title: "How it works", description: "Follow the Trancense chain from evidence to approved data, reproducible calculation, accountable action, and measured verification.", alternates: { canonical: "/how-it-works" } };
export default function How(){return <PublicPage><section className="shell section"><p className="kicker">How it works</p><h1 className="mt-4 max-w-3xl">A chain that makes the next decision easier to examine.</h1></section><section className="section-band section-band-muted"><ol className="shell section chain-grid">{siteContent.chain.map((step,index)=><li key={step}><span>0{index+1}</span><h2 className="mt-8 text-2xl">{step}</h2><p>{index===0?'Preserve source context and original units.':index===1?'Separate reviewed compatible records from draft work.':index===2?'Show formulas, factors, scope, and exclusions.':index===3?'Keep support and confidence visible.':index===4?'Assign and track work without implying measured outcomes.':'Record measured verification distinctly when it exists.'}</p></li>)}</ol></section><section className="shell section"><ActionLink href="/product" variant="secondary">Explore the platform</ActionLink></section></PublicPage>}
