import type { Metadata } from "next"; import { ActionLink } from "@/components/ui"; import { PublicPage } from "@/components/public-page";
export const metadata: Metadata = { title: "For facility teams and reviewers", description: "Review approved data, decisions, limits, and action status in one accountable energy-audit workspace.", alternates: { canonical: "/facility-teams" } };
export default function FacilityTeams(){return <PublicPage><section className="shell section"><p className="kicker">For facility teams and reviewers</p><h1 className="mt-4 max-w-3xl">See the evidence, decision boundary, and action status in one reviewable place.</h1><div className="mt-12 grid gap-8 md:grid-cols-2"><div className="data-card p-8"><p className="kicker">A practical review</p><p className="mt-4">Approved data, calculations, findings, and action states are visibly distinct. Completed work is not represented as measured verification.</p></div><p className="max-w-lg text-lg muted">Reviewers can work from authorized data without losing the limits, assumptions, or source trail that make a conclusion accountable.</p></div><ActionLink href="/product" variant="secondary" className="mt-10">Explore the platform</ActionLink></section></PublicPage>}
