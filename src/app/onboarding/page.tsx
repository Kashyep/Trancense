import Link from 'next/link'
import { createPilotWorkspace } from '@/app/actions'
import { isSupabaseConfigured } from '@/lib/supabase/server'

export default function OnboardingPage() {
  if (!isSupabaseConfigured()) return <div className="auth-page"><div className="auth-card"><Link href="/" className="brand"><span className="brand-mark"/>Trancense</Link><div style={{ marginTop: 30 }}><p className="eyebrow">Demo onboarding</p><h1>Set up your pilot workspace.</h1><p className="auth-subtitle">Supabase is not configured in this environment, so the local demo workspace is already available.</p><Link className="button button-primary" href="/dashboard">Open demo workspace →</Link></div></div></div>
  return <div className="auth-page"><div className="auth-card"><Link href="/" className="brand"><span className="brand-mark"/>Trancense</Link><div style={{ marginTop: 30 }}><p className="eyebrow">First assessment</p><h1>Set up your workspace.</h1><p className="auth-subtitle">Start with one client site, one facility, and one active audit. You can expand the model later.</p></div><form className="form-stack" action={createPilotWorkspace}><div className="field"><label htmlFor="workspaceName">Workspace name</label><input id="workspaceName" name="workspaceName" required placeholder="Your consulting practice"/></div><div className="field"><label htmlFor="siteName">Site name</label><input id="siteName" name="siteName" required placeholder="Client site"/></div><div className="field"><label htmlFor="facilityName">Facility name</label><input id="facilityName" name="facilityName" required placeholder="Building or plant"/></div><button className="button button-primary" type="submit">Create pilot workspace →</button></form><div className="auth-foot">India-first · INR · Monthly reporting · Human reviewed</div></div></div>
}
